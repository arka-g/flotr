<%= will_paginate @images %>

<% @images.each do |img| %>
  <%= image_tag img.image.url(:large) %>
<% end %>
<br>
<%= link_to 'Browse Tags', browse_path %>

<%= javascript_include_tag "indexJavascript" %>
<%= javascript_tag do %>
  setPageLimit(<%= @images.total_pages %>);
<% end %>
